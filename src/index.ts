import { writeFsmap, readFsmap, diffFsmap } from "./fsmap";
import { dumpFsmap, minifyFsmap, summarizeFsmap } from "./dumper";

export default {
  writeFsmap,
  readFsmap,
  diffFsmap,
  dumpFsmap,
  minifyFsmap,
  summarizeFsmap,
};
